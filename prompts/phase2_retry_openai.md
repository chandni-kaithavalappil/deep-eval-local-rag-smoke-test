# Phase 2 Retry OpenAI Prompt

PHASE 2 RETRY: .env now has a fresh OPENAI_API_KEY. Re-run the Phase 2 plan from scratch: (a) Verify .env's OPENAI_API_KEY does not start with "sk-replace" — if it does, STOP. (b) Sanity-check imports: `UV_CACHE_DIR="$PWD/.uv-cache" uv run python -c "import lightrag, deepeval; print('ok')"`. (1) Run `bash docs/fetch_corpus.sh` and confirm at least 3 .md files in docs/. If fewer than 3, STOP and list which URLs failed. (2) Run `UV_CACHE_DIR="$PWD/.uv-cache" uv run python src/ingest.py` and stream output. (3) Verify rag_storage/ has files like kv_store_*.json, vdb_chunks.json, graph_chunk_entity_relation.graphml. (4) Print exactly: "PHASE 2 COMPLETE. Corpus ingested. Next: Phase 3 — handcrafted eval." If anything errors, paste the full traceback and stop without retrying. Keep UV_CACHE_DIR="$PWD/.uv-cache" on every uv command.
