# Phase 1 Setup Prompt

Set up a Sunday experiment that evaluates LightRAG with DeepEval. The scaffolding files (pyproject.toml, .env.example, .gitignore, README.md, src/*.py, tests/handcrafted_qa.json, docs/fetch_corpus.sh) are already pre-built at this path: "/Users/chandnikaithavalappil/Library/Application Support/Claude/local-agent-mode-sessions/54fcb717-ca08-4c27-ba49-a1b965d15656/89b6a9dd-8887-4628-99e3-e4762113815d/local_27c1c49f-9fc4-44f3-8e7c-08eaa6e78605/outputs/scaffolding". Do EXACTLY these steps in order, in the current working directory: (1) Verify the scaffolding path above exists and list its contents with `ls -la`. If missing, STOP and tell me. (2) Copy every file and directory from that scaffolding folder into the current directory, preserving structure and including hidden files (.env.example, .gitignore) — use `cp -R "<path>/." .` or rsync. (3) Create empty `rag_storage/` and `results/` directories. (4) `chmod +x docs/fetch_corpus.sh`. (5) Copy `.env.example` to `.env`. (6) If `uv` is not installed, install it via `curl -LsSf https://astral.sh/uv/install.sh | sh` and source its env. (7) Run `uv venv --python 3.11`, then `uv pip install -e .`. If that fails, fall back to `uv pip install lightrag-hku deepeval python-dotenv openai`. (8) Print the tree: `find . -maxdepth 3 -not -path '*/.venv*' -not -path '*/.git*' | sort` and the contents of pyproject.toml. (9) Print exactly: "PHASE 1 COMPLETE. Next: edit .env to set OPENAI_API_KEY, then ask me to run Phase 2." DO NOT run ingest.py, any eval script, or fetch_corpus.sh — those are later phases.
