# Phase 2 OpenAI Prompt

PHASE 2: Fetch the Anthropic corpus and ingest it into LightRAG. First do prerequisite checks: (a) Verify `.env` exists and OPENAI_API_KEY is set to something that does NOT start with "sk-replace" — if the placeholder is still there, STOP and tell me. (b) Verify the venv works and packages import: run `UV_CACHE_DIR="$PWD/.uv-cache" uv run python -c "import lightrag, deepeval; print('lightrag', getattr(lightrag, '__version__', '?'), 'deepeval', getattr(deepeval, '__version__', '?'))"`. Then: (1) Fetch the corpus by running `bash docs/fetch_corpus.sh`. Network access is required. The script tolerates individual URL failures — if at least 3 .md files end up in docs/, proceed. After it finishes run `ls -lh docs/*.md` and report file count + total size. If fewer than 3 .md files exist, STOP and report which URLs failed. (2) Ingest into LightRAG by running `UV_CACHE_DIR="$PWD/.uv-cache" uv run python src/ingest.py`. This will hit the OpenAI API for embeddings + entity extraction; expect 1–3 minutes and a few hundred small API calls. Stream the output as it runs — do not silence it. (3) After ingest completes successfully, verify the index by listing `ls -lh rag_storage/` and confirm files like `kv_store_*.json`, `vdb_chunks.json`, and `graph_chunk_entity_relation.graphml` are present and non-empty. (4) Print exactly: "PHASE 2 COMPLETE. Corpus ingested into rag_storage/. Next: Phase 3 — run the handcrafted eval." If the ingest script throws an error, paste the FULL traceback, stop, and do not retry without my instruction. Keep `UV_CACHE_DIR="$PWD/.uv-cache"` for every `uv` command in this phase.
