# Phase 2 Ollama Prompt

PHASE 2 (Ollama): Run the full local-Ollama ingest. (a) Confirm Ollama is running: `curl -sS http://localhost:11434/api/tags >/dev/null && echo "ollama ok"`. (b) Confirm rag_storage/ is empty (per learner.md, partial files break dim invariants): `ls rag_storage/ | wc -l` should print 0. If not empty, STOP and report. (c) Smoke-test the full local config: `UV_CACHE_DIR="$PWD/.uv-cache" uv run --no-sync python src/check_config.py --embed --llm --judge`. Must pass; if dim mismatch or judge error, STOP and report. (1) Run the ingest with output streaming: `UV_CACHE_DIR="$PWD/.uv-cache" uv run --no-sync python src/ingest.py 2>&1 | tee results/ingest.log`. Expect 5-15 minutes given local model speed. (2) After it finishes, DO NOT trust the `Done` line alone — scan results/ingest.log for any "Traceback", "ERROR", or "extraction" failure lines: `grep -nE "Traceback|ERROR|Failed|Exception" results/ingest.log || echo "no errors detected"`. (3) Verify the index is populated and non-empty: `ls -lh rag_storage/` and check for `kv_store_*.json`, `vdb_chunks.json`, `graph_chunk_entity_relation.graphml`, each > 1KB. (4) Print exactly: "PHASE 2 OLLAMA COMPLETE. Index built with granite4.1:8b + qwen3-embedding:4b (2560-dim)." If any traceback found OR any expected index file is missing/empty, STOP and paste the relevant log lines.
