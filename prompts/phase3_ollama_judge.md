# Phase 3 Ollama Judge Prompt

PHASE 3 (Ollama judge): Run the handcrafted eval using the local DeepEval judge (granite4.1:8b with capability metadata = granite4). (1) Confirm DEEPEVAL_PROVIDER=ollama in .env (`grep DEEPEVAL_PROVIDER .env`). (2) Confirm rag_storage/ has the expected files from Phase 2 (`ls rag_storage/`). (3) Run: `UV_CACHE_DIR="$PWD/.uv-cache" uv run --no-sync python src/eval_handcrafted.py 2>&1 | tee results/eval_handcrafted.log`. With 9 cases × 5 metrics × local judge, expect 15-40 minutes. (4) Verify `results/handcrafted_scores.json` exists and is non-empty. (5) Print the summary: `UV_CACHE_DIR="$PWD/.uv-cache" uv run --no-sync python -c "import json; from collections import defaultdict; data=json.load(open('results/handcrafted_scores.json')); agg=defaultdict(list); [agg[m].append(s) for d in data for m,s in d['scores'].items() if s is not None]; print('\n=== HANDCRAFTED EVAL SUMMARY ==='); [print(f'{m:30s} avg={sum(v)/len(v):.3f}  pass(>=0.7)={sum(1 for x in v if x>=0.7)}/{len(v)}') for m,v in agg.items()]; print(f'\nTotal cases: {len(data)}')"`. (6) Print exactly: "PHASE 3 OLLAMA COMPLETE. Next: Phase 4 — synthesize + synthesized eval." If any single metric returns all None / NaN, STOP and paste the relevant DeepEval error lines from results/eval_handcrafted.log.
